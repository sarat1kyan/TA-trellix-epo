# Trellix ePO CIM Normalization Transforms
# Maps ePO events to Common Information Model (CIM) fields

# Threat Events -> Intrusion_Detection
[trellix_epo_threat_to_ids]
REGEX = (?i)"threatType"\s*:\s*"([^"]+)"|"severity"\s*:\s*"([^"]+)"|"detectionId"\s*:\s*"?([^",}]+)
FORMAT = threat_type::$1 severity::$2 detection_id::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Intrusion_Detection

[trellix_epo_threat_cim_fields]
REGEX = 
FORMAT = 
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Intrusion_Detection

# Malware Detections -> Malware
[trellix_epo_malware_to_malware]
REGEX = (?i)"malwareType"\s*:\s*"([^"]+)"|"fileHash"\s*:\s*"([^"]+)"|"filePath"\s*:\s*"([^"]+)
FORMAT = malware_type::$1 file_hash::$2 file_path::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Malware

[trellix_epo_malware_cim_fields]
REGEX = 
FORMAT = 
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Malware

# Host Status -> Endpoint
[trellix_epo_host_to_endpoint]
REGEX = 
FORMAT = 
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Endpoint

# Policy Compliance -> Change
[trellix_epo_policy_to_change]
REGEX = (?i)"policyName"\s*:\s*"([^"]+)"|"complianceStatus"\s*:\s*"([^"]+)"|"violationCount"\s*:\s*"?([^",}]+)
FORMAT = policy_name::$1 compliance_status::$2 violation_count::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Change

# User Actions -> Audit
[trellix_epo_user_to_audit]
REGEX = (?i)"action"\s*:\s*"([^"]+)"|"result"\s*:\s*"([^"]+)"|"userName"\s*:\s*"([^"]+)
FORMAT = action::$1 result::$2 user::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Audit

# Field Extractions - Note: JSON is extracted automatically via KV_MODE=json in props.conf
# These are fallback extractions for non-standard JSON formats

[threat_extractions]
# Combined extraction for threat events - extracts multiple fields
REGEX = "detectionId"\s*:\s*"?(?<detection_id>[^",}]+)"?.*?"threatName"\s*:\s*"(?<threat_name>[^"]+)".*?"severity"\s*:\s*"(?<severity>[^"]+)"
FORMAT = detection_id::$1 threat_name::$2 severity::$3

[threat_extractions_detection_id]
REGEX = "detectionId"\s*:\s*"?([^",}]+)"?
FORMAT = detection_id::$1

[threat_extractions_threat_name]
REGEX = "threatName"\s*:\s*"([^"]+)"
FORMAT = threat_name::$1

[threat_extractions_severity]
REGEX = "severity"\s*:\s*"([^"]+)"
FORMAT = severity::$1

[malware_extractions]
# Combined extraction for malware events - extracts multiple fields
REGEX = "malwareName"\s*:\s*"(?<malware_name>[^"]+)".*?"filePath"\s*:\s*"(?<file_path>[^"]+)".*?"fileHash"\s*:\s*"(?<file_hash>[^"]+)"
FORMAT = malware_name::$1 file_path::$2 file_hash::$3

[malware_extractions_malware_name]
REGEX = "malwareName"\s*:\s*"([^"]+)"
FORMAT = malware_name::$1

[malware_extractions_file_hash]
REGEX = "fileHash"\s*:\s*"([^"]+)"
FORMAT = file_hash::$1

[malware_extractions_file_path]
REGEX = "filePath"\s*:\s*"([^"]+)"
FORMAT = file_path::$1

# CIM Field Mappings
# Note: CIM fields are primarily mapped via FIELDALIAS in props.conf
# These transforms provide additional extraction capabilities

[cim_intrusion_detection]
# Extract severity for CIM Intrusion_Detection data model
REGEX = "severity"\s*:\s*"([^"]+)"
FORMAT = severity::$1

[cim_intrusion_detection_signature]
REGEX = "threatName"\s*:\s*"([^"]+)"
FORMAT = signature::$1

[cim_intrusion_detection_signature_id]
REGEX = "detectionId"\s*:\s*"?([^",}]+)"?
FORMAT = signature_id::$1

[cim_intrusion_detection_dest]
REGEX = "computerName"\s*:\s*"([^"]+)"
FORMAT = dest_host::$1

[cim_malware]
# Extract malware name for CIM Malware data model
REGEX = "malwareName"\s*:\s*"([^"]+)"
FORMAT = malware_name::$1

[cim_malware_hash]
REGEX = "fileHash"\s*:\s*"([^"]+)"
FORMAT = file_hash::$1

[cim_malware_path]
REGEX = "filePath"\s*:\s*"([^"]+)"
FORMAT = file_path::$1

[cim_endpoint]
# Extract host info for CIM Endpoint data model
REGEX = "computerName"\s*:\s*"([^"]+)"
FORMAT = dest_host::$1

[cim_endpoint_ip]
REGEX = "ipAddress"\s*:\s*"([^"]+)"
FORMAT = dest_ip::$1

[cim_endpoint_os]
REGEX = "operatingSystem"\s*:\s*"([^"]+)"
FORMAT = dest_os::$1

[cim_audit]
# Extract user for CIM Authentication/Audit data model
REGEX = "userName"\s*:\s*"([^"]+)"
FORMAT = user::$1

[cim_audit_action]
REGEX = "action"\s*:\s*"([^"]+)"
FORMAT = action::$1

[cim_audit_result]
REGEX = "result"\s*:\s*"([^"]+)"
FORMAT = result::$1

[cim_audit_src_ip]
REGEX = "sourceIP"\s*:\s*"([^"]+)"
FORMAT = src_ip::$1

# CIM Normalization for Syslog-based Threat Events
[cim_syslog_intrusion_detection]
REGEX = (?i)(?:threat[_\s]?name|malware)[:=]\s*["']?([^"',\n]+)["']?
FORMAT = signature::$1
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Intrusion_Detection

[cim_syslog_malware]
REGEX = (?i)(?:threat[_\s]?name|malware)[:=]\s*["']?([^"',\n]+)["']?
FORMAT = malware_name::$1
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Malware

[cim_syslog_malware_hash]
REGEX = (?i)(?:hash|md5|sha1|sha256)[:=]\s*([a-fA-F0-9]{32,64})
FORMAT = file_hash::$1

[cim_syslog_malware_path]
REGEX = (?i)(?:file|path|filepath)[:=]\s*["']?([^"',\n]+)["']?
FORMAT = file_path::$1

# ============================================
# NEW v2.0.0 CIM TRANSFORMS
# ============================================

# Threat Summary -> Intrusion_Detection
[trellix_epo_threat_summary_to_ids]
REGEX = "threatStatus"\s*:\s*"([^"]+)"|"severity"\s*:\s*"([^"]+)"|"threatName"\s*:\s*"([^"]+)"
FORMAT = threat_status::$1 severity::$2 signature::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Intrusion_Detection

[cim_threat_summary_signature]
REGEX = "threatName"\s*:\s*"([^"]+)"
FORMAT = signature::$1

[cim_threat_summary_action]
REGEX = "action"\s*:\s*"([^"]+)"
FORMAT = action::$1

# DLP Incidents -> DLP
[trellix_epo_dlp_to_dlp]
REGEX = "incidentId"\s*:\s*"?([^",}]+)"?|"severity"\s*:\s*"([^"]+)"|"ruleName"\s*:\s*"([^"]+)"
FORMAT = incident_id::$1 severity::$2 policy::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = DLP

[cim_dlp_src]
REGEX = "sourceHost"\s*:\s*"([^"]+)"
FORMAT = src::$1

[cim_dlp_dest]
REGEX = "destination"\s*:\s*"([^"]+)"
FORMAT = dest::$1

[cim_dlp_user]
REGEX = "userName"\s*:\s*"([^"]+)"
FORMAT = user::$1

[cim_dlp_action]
REGEX = "actionTaken"\s*:\s*"([^"]+)"
FORMAT = action::$1

[cim_dlp_classification]
REGEX = "dataClassification"\s*:\s*"([^"]+)"
FORMAT = classification::$1

# Web Control Events -> Web
[trellix_epo_web_to_web]
REGEX = "url"\s*:\s*"([^"]+)"|"category"\s*:\s*"([^"]+)"|"action"\s*:\s*"([^"]+)"
FORMAT = url::$1 category::$2 action::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Web

[cim_web_url]
REGEX = "url"\s*:\s*"([^"]+)"
FORMAT = url::$1

[cim_web_category]
REGEX = "category"\s*:\s*"([^"]+)"
FORMAT = category::$1

[cim_web_action]
REGEX = "action"\s*:\s*"([^"]+)"
FORMAT = action::$1

[cim_web_user]
REGEX = "userName"\s*:\s*"([^"]+)"
FORMAT = user::$1

# Firewall Events -> Network_Traffic
[trellix_epo_firewall_to_network]
REGEX = "ruleName"\s*:\s*"([^"]+)"|"action"\s*:\s*"([^"]+)"|"protocol"\s*:\s*"([^"]+)"
FORMAT = rule::$1 action::$2 transport::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Network_Traffic

[cim_firewall_src]
REGEX = "sourceIp"\s*:\s*"([^"]+)"
FORMAT = src_ip::$1

[cim_firewall_dest]
REGEX = "destIp"\s*:\s*"([^"]+)"
FORMAT = dest_ip::$1

[cim_firewall_port]
REGEX = "port"\s*:\s*"?([^",}]+)"?
FORMAT = dest_port::$1

[cim_firewall_direction]
REGEX = "direction"\s*:\s*"([^"]+)"
FORMAT = direction::$1

# Application Control Events -> Endpoint.Processes
[trellix_epo_app_control_to_endpoint]
REGEX = "application"\s*:\s*"([^"]+)"|"action"\s*:\s*"([^"]+)"|"path"\s*:\s*"([^"]+)"
FORMAT = process::$1 action::$2 process_path::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Endpoint

[cim_app_control_process]
REGEX = "application"\s*:\s*"([^"]+)"
FORMAT = process::$1

[cim_app_control_path]
REGEX = "path"\s*:\s*"([^"]+)"
FORMAT = process_path::$1

[cim_app_control_hash]
REGEX = "checksum"\s*:\s*"([^"]+)"
FORMAT = process_hash::$1

[cim_app_control_user]
REGEX = "userName"\s*:\s*"([^"]+)"
FORMAT = user::$1

# EDR Events -> Intrusion_Detection / Malware
[trellix_epo_edr_to_ids]
REGEX = "eventType"\s*:\s*"([^"]+)"|"reputation"\s*:\s*"([^"]+)"|"threatName"\s*:\s*"([^"]+)"
FORMAT = category::$1 reputation::$2 signature::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Intrusion_Detection

[cim_edr_process]
REGEX = "processName"\s*:\s*"([^"]+)"
FORMAT = process::$1

[cim_edr_hash]
REGEX = "fileHash"\s*:\s*"([^"]+)"
FORMAT = file_hash::$1

[cim_edr_path]
REGEX = "filePath"\s*:\s*"([^"]+)"
FORMAT = file_path::$1

[cim_edr_action]
REGEX = "action"\s*:\s*"([^"]+)"
FORMAT = action::$1

# Device Management -> Endpoint
[trellix_epo_device_to_endpoint]
REGEX = "protectionStatus"\s*:\s*"([^"]+)"|"agentStatus"\s*:\s*"([^"]+)"|"managedState"\s*:\s*"?([^",}]+)"?
FORMAT = protection_status::$1 agent_status::$2 managed_state::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Endpoint

[cim_device_host]
REGEX = "computerName"\s*:\s*"([^"]+)"
FORMAT = dest_host::$1

[cim_device_ip]
REGEX = "ipAddress"\s*:\s*"([^"]+)"
FORMAT = dest_ip::$1

# Software Status -> Inventory
[trellix_epo_software_to_inventory]
REGEX = "productName"\s*:\s*"([^"]+)"|"productVersion"\s*:\s*"([^"]+)"|"deploymentStatus"\s*:\s*"([^"]+)"
FORMAT = product::$1 version::$2 status::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Inventory

[cim_software_product]
REGEX = "productName"\s*:\s*"([^"]+)"
FORMAT = product::$1

[cim_software_version]
REGEX = "productVersion"\s*:\s*"([^"]+)"
FORMAT = version::$1

# Compliance Overview -> Change
[trellix_epo_compliance_to_change]
REGEX = "complianceStatus"\s*:\s*"([^"]+)"|"contentType"\s*:\s*"([^"]+)"|"compliancePct"\s*:\s*"?([^",}]+)"?
FORMAT = status::$1 object_category::$2 result::$3
DEST_KEY = MetaData:CIM
WRITE_META = true
DEFAULT_VALUE = Change

[cim_compliance_status]
REGEX = "complianceStatus"\s*:\s*"([^"]+)"
FORMAT = status::$1

[cim_compliance_type]
REGEX = "contentType"\s*:\s*"([^"]+)"
FORMAT = object_category::$1

# ============================================
# END OF NEW v2.0.0 CIM TRANSFORMS
# ============================================

