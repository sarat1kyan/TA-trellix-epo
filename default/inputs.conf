# Trellix ePO Modular Input Configuration
# Configure your ePO server settings first via the Setup UI or in ta_trellix_epo_settings.conf
# Then enable the inputs you need by setting disabled = 0
#
# To enable an input:
#   1. Copy the desired stanza(s) to local/inputs.conf
#   2. Set disabled = 0
#   3. Adjust interval and index as needed

# ============================================
# Modular Input Type Definition
# Required for Splunk Cloud compatibility
# ============================================
[trellix_epo]
python.version = python3

# ============================================
# Threat Events - Real-time threat detection
# Recommended interval: 300 seconds (5 minutes)
# Priority: HIGH - Enable first for security monitoring
# ============================================
[trellix_epo://threat_events]
python.version = python3
disabled = 1
interval = 300
index = main
sourcetype = trellix_epo:threat_events
input_type = threat_events

# ============================================
# Malware Detections - Malware identification
# Recommended interval: 300 seconds (5 minutes)
# Priority: HIGH - Essential for threat detection
# ============================================
[trellix_epo://malware_detections]
python.version = python3
disabled = 1
interval = 300
index = main
sourcetype = trellix_epo:malware_detections
input_type = malware_detections

# ============================================
# Host Status - System health information
# Recommended interval: 3600 seconds (1 hour)
# Priority: MEDIUM - Endpoint inventory
# ============================================
[trellix_epo://host_status]
python.version = python3
disabled = 1
interval = 3600
index = main
sourcetype = trellix_epo:host_status
input_type = host_status

# ============================================
# Agent Status - ePO agent connectivity
# Recommended interval: 3600 seconds (1 hour)
# Priority: MEDIUM - Agent health monitoring
# ============================================
[trellix_epo://agent_status]
python.version = python3
disabled = 1
interval = 3600
index = main
sourcetype = trellix_epo:agent_status
input_type = agent_status

# ============================================
# Policy Compliance - Policy violations
# Recommended interval: 7200 seconds (2 hours)
# Priority: MEDIUM - Compliance monitoring
# ============================================
[trellix_epo://policy_compliance]
python.version = python3
disabled = 1
interval = 7200
index = main
sourcetype = trellix_epo:policy_compliance
input_type = policy_compliance

# ============================================
# Quarantine Events - Quarantine actions
# Recommended interval: 600 seconds (10 minutes)
# Priority: HIGH - Track remediation actions
# ============================================
[trellix_epo://quarantine_events]
python.version = python3
disabled = 1
interval = 600
index = main
sourcetype = trellix_epo:quarantine_events
input_type = quarantine_events

# ============================================
# Updates/DAT Versions - Signature updates
# Recommended interval: 14400 seconds (4 hours)
# Priority: LOW - DAT version tracking
# ============================================
[trellix_epo://updates]
python.version = python3
disabled = 1
interval = 14400
index = main
sourcetype = trellix_epo:updates
input_type = updates

# ============================================
# User Actions - Audit logs
# Recommended interval: 600 seconds (10 minutes)
# Priority: MEDIUM - Administrative audit trail
# ============================================
[trellix_epo://user_actions]
python.version = python3
disabled = 1
interval = 600
index = main
sourcetype = trellix_epo:user_actions
input_type = user_actions

# ============================================
# NEW v2.0.0 INPUT TYPES
# ============================================

# ============================================
# Threat Summary - Escalated, Resolved, Unresolved threats
# Recommended interval: 300 seconds (5 minutes)
# Priority: HIGH - Threat status overview
# Tables: PWS_ThreatSummary, PWS_Threat, EPExtendedEvent
# ============================================
[trellix_epo://threat_summary]
python.version = python3
disabled = 1
interval = 300
index = main
sourcetype = trellix_epo:threat_summary
input_type = threat_summary

# ============================================
# Software Status - Product deployment status
# Recommended interval: 3600 seconds (1 hour)
# Priority: MEDIUM - Software inventory
# Tables: EPOSystemProductVersionInfo, EPORollup_ProductProperties*
# ============================================
[trellix_epo://software_status]
python.version = python3
disabled = 1
interval = 3600
index = main
sourcetype = trellix_epo:software_status
input_type = software_status

# ============================================
# Compliance Overview - Security content compliance
# Recommended interval: 3600 seconds (1 hour)
# Priority: MEDIUM - Compliance monitoring
# Tables: EpoRollup_ComplianceHistory, SCOR_VW_NON_COMPLIANT_AGENT
# ============================================
[trellix_epo://compliance_overview]
python.version = python3
disabled = 1
interval = 3600
index = main
sourcetype = trellix_epo:compliance_overview
input_type = compliance_overview

# ============================================
# DLP Incidents - Data Loss Prevention events
# Recommended interval: 600 seconds (10 minutes)
# Priority: HIGH - Data protection monitoring
# Tables: UDLP_EPD_Incidents, UDLP_IncidentsQueriesView
# ============================================
[trellix_epo://dlp_incidents]
python.version = python3
disabled = 1
interval = 600
index = main
sourcetype = trellix_epo:dlp_incidents
input_type = dlp_incidents

# ============================================
# Device Management - Check-in failures, protection status
# Recommended interval: 1800 seconds (30 minutes)
# Priority: MEDIUM - Device health monitoring
# Tables: MAEnforcementStatusView, MARebootPendingView
# ============================================
[trellix_epo://device_management]
python.version = python3
disabled = 1
interval = 1800
index = main
sourcetype = trellix_epo:device_management
input_type = device_management

# ============================================
# EDR Events - Endpoint Detection and Response
# Recommended interval: 300 seconds (5 minutes)
# Priority: HIGH - Advanced threat detection
# Tables: MVEDRCustomEvent, MVEDRProperties, TieServerSchema.*
# ============================================
[trellix_epo://edr_events]
python.version = python3
disabled = 1
interval = 300
index = main
sourcetype = trellix_epo:edr_events
input_type = edr_events

# ============================================
# Web Control Events - URL filtering
# Recommended interval: 600 seconds (10 minutes)
# Priority: MEDIUM - Web security monitoring
# Tables: WP_EventInfo
# ============================================
[trellix_epo://web_control_events]
python.version = python3
disabled = 1
interval = 600
index = main
sourcetype = trellix_epo:web_control_events
input_type = web_control_events

# ============================================
# Firewall Events - Firewall rules and events
# Recommended interval: 1800 seconds (30 minutes)
# Priority: LOW - Firewall configuration
# Tables: FW_Rule, FW_NamedNetwork, FW_Application
# ============================================
[trellix_epo://firewall_events]
python.version = python3
disabled = 1
interval = 1800
index = main
sourcetype = trellix_epo:firewall_events
input_type = firewall_events

# ============================================
# Application Control Events - Solidcore
# Recommended interval: 600 seconds (10 minutes)
# Priority: MEDIUM - Application whitelisting
# Tables: SCOR_EVENTS, SCOR_VW_INVENTORY, SCOR_ALERTS
# ============================================
[trellix_epo://app_control_events]
python.version = python3
disabled = 1
interval = 600
index = main
sourcetype = trellix_epo:app_control_events
input_type = app_control_events
