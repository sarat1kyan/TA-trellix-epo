[install]
is_configured = 0
state = enabled
build = 1
check_for_updates = 1

[package]
check_for_updates = 1
id = TA-trellix-epo

[ui]
is_visible = 1
label = Trellix ePO Add-on
setup_view = setup_page

[launcher]
author = Mher Saratikyan
description = Technology Add-on for Trellix (McAfee) ePO - Ingests security telemetry via REST API and syslog, normalizes to CIM for Enterprise Security. Includes comprehensive security dashboard with threat intelligence, endpoint health, and compliance monitoring.
version = 1.2.2

[id]
name = TA-trellix-epo
version = 1.2.2

[triggers]
reload.addon = simple
reload.ta_trellix_epo_settings = simple
reload.ta_trellix_epo_inputs = simple

[logging]
level = INFO

