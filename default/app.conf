[install]
is_configured = 0
state = enabled
build = 1
check_for_updates = 1

[package]
check_for_updates = 1
id = TA-trellix-epo

[ui]
is_visible = 1
label = Trellix ePO Add-on
setup_view = setup_page

[launcher]
author = Mher Saratikyan
description = Technology Add-on for Trellix (McAfee) ePO - Ingests security telemetry via REST API and syslog, normalizes to CIM for Enterprise Security. v2.0 adds threat summary, software status, compliance overview, DLP incidents, device management, EDR, web control, firewall, and application control inputs.
version = 2.0.0

[id]
name = TA-trellix-epo
version = 2.0.0

[triggers]
reload.addon = simple
reload.ta_trellix_epo_settings = simple
reload.ta_trellix_epo_inputs = simple

[logging]
level = INFO

