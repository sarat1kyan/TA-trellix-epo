# Trellix ePO Splunk Add-on Requirements
# Python dependencies for the Technology Add-on

# HTTP library for REST API interactions
requests>=2.28.0

# Splunk SDK - Required for configure_credentials.py script
# Usually bundled with Splunk, but can be installed separately if needed:
#   pip install splunk-sdk
splunk-sdk>=1.7.0

# Optional: For enhanced SSL/TLS support
urllib3>=1.26.0

# Optional: For better datetime handling (if not using standard library)
# pytz>=2022.1

# Note: splunklib is typically bundled with Splunk Enterprise
# Ensure Python 3 is available (Splunk 8.0+)

